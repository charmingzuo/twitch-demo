mixin image(url)
    if url
        - var picSize = url.match(/(\d+)x(\d+)\.(jpg|png)/i) || [];
        - var picWidth = picSize[1] || '';
        - var picHeight = picSize[2] || '';
        img(src=url, style='width:' + picWidth + 'px; height:' + picHeight + 'px;')