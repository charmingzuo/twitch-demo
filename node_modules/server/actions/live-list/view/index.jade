extends ../../../common/view/template.jade

block title
    |鹅蛋

block header
    script.
        console.log('this is header.');

block body
    - template = locals.data.template;
    - page = locals.data.page;

    h1= template.title

    ul
        each live in page.liveList
            li
                =live.title